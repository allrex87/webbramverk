<h1>Skapa nytt projekt</h1>

<p> Eller vill du lägga till dig som medlem på ett befintligt projekt?</p>


<%# render "_ecPartial"%>
<%= form_for @project_to_create, :url => { :action => "create"}, :html => { :class => "create_form"} do |f| %>
	
	<%= f.label :project_name, "Projektnamn" %>
		<%= f.text_field :project_name %>
		
	<%= f.label :project_name, "Projektnamn" %>
		<%= f.text_field :project_name %>
		
	<%= f.label :project_name, "Projektnamn" %>
		<%= f.text_field :project_name %>
		
	<%= f.label :project_name, "Projektnamn" %>
		<%= f.text_field :project_name %>
		
	<%= f.label :project_name, "Projektnamn" %>
		<%= f.text_field :project_name %>
		
	<%= submit_tag("Skapa") %>
<% end %>